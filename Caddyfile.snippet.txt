# Add INSIDE the `http://studio.oratelecom.net { ... }` block
# in ~/www/private/ora/ora_callcenter/Caddyfile, then reload caddy.

handle_path /slimstack/* {
    root * /srv/ora-sites/slimstack
    try_files {path} /index.html
    file_server
}
redir /slimstack /slimstack/ 301

# Deploy on server side:
#   rsync -a ~/www/public/slimstack/ user@studio.oratelecom.net:/srv/ora-sites/slimstack/
#   sudo systemctl reload caddy
